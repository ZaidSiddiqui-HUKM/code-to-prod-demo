# This file makes the repo root importable, so `pytest` finds `app.main`
# from the tests. Without it the Test stage fails to even collect the tests.
# Nothing to configure - its presence is the whole point.
